<%= form_for(@classificacao_anexo_inquerito) do |f| %>
  <% if @classificacao_anexo_inquerito.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@classificacao_anexo_inquerito.errors.count, "erro(s)") %> impediram esta Classificação de Anexo de Inquerito de ser salva:</h2>

      <ul>
      <% @classificacao_anexo_inquerito.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao, "Nome" %><br>
    <%= f.text_field :descricao %>
  </div>
  <div class="actions">
    <%= f.submit "Salvar", class: "btn btn-success" %>
  </div>
<% end %>
